class GeneralActuator


instance variables

protected ID	: nat;
protected Type	: GlobalTypes`nodeType;
protected Corr	: GlobalTypes`correction;
protected Env	: Environment;


operations

public GetID: () ==> nat
	GetID() ==
		return ID;

public GetType: () ==> GlobalTypes`nodeType
	GetType() ==
		return Type;

public Step: () ==> ()
	Step() ==
		is subclass responsibility

end GeneralActuator